schema
  @server(port: 8003)
  @upstream(baseURL: "http://127.0.0.1:8083") {
  query: Query
}

type Query {
  productTypes: [Products!]!
    @discriminate(field: "type")
    @http(baseURL: "http://127.0.0.1:8083", path: "/products")
  topSecretFederationFacts: [TopSecretFact!]!
    @http(baseURL: "http://127.0.0.1:8083", path: "/top_secret_facts")
    @discriminate(field: "type")
  factTypes: [TopSecretFactType!]
    @http(baseURL: "http://127.0.0.1:8083", path: "/fact_types")
}

enum TopSecretFactType {
  DIRECTIVE
  ENTITY
  MISCELLANEOUS
}

interface TopSecretFact {
  description: FactContent!
  factType: TopSecretFactType
}

scalar FactContent

type DirectiveFact implements TopSecretFact {
  title: String!
  description: FactContent!
  factType: TopSecretFactType
}

type EntityFact implements TopSecretFact {
  title: String!
  description: FactContent!
  factType: TopSecretFactType
}

type MiscellaneousFact implements TopSecretFact {
  title: String!
  description: FactContent!
  factType: TopSecretFactType
}

enum ProductName {
  CONSULTANCY
  COSMO
  ENGINE
  FINANCE
  HUMAN_RESOURCES
  MARKETING
  SDK
}

type Employee {
  id: Int!
  products: [ProductName!]!
    @http(
      baseURL: "http://127.0.0.1:8083"
      path: "/products/employees/{{.value.id}}"
      select: "{{.products}}"
    )
  notes: String
    @http(
      baseURL: "http://127.0.0.1:8083"
      path: "/products/employees/{{.value.id}}"
      select: "{{.notes}}"
    )
}

union Products = Consultancy | Cosmo | Documentation

type Consultancy {
  upc: ID!
  name: ProductName!
    @http(
      baseURL: "http://127.0.0.1:8083"
      path: "/products/{{.value.upc}}"
      select: "{{.name}}"
    )
}

type Cosmo {
  upc: ID!
  name: ProductName!
    @http(
      baseURL: "http://127.0.0.1:8083"
      path: "/products/{{.value.upc}}"
      select: "{{.name}}"
    )
  repositoryUrl: String!
    @http(
      baseURL: "http://127.0.0.1:8083"
      path: "/products/{{.value.upc}}"
      select: "{{.repositoryUrl}}"
    )
}

type Documentation {
  url(product: ProductName!): String!
  urls(products: [ProductName!]!): [String!]!
}
